/* --- 1. CORE INTERFACE --- */
.yomitan-popup {
  background: #ffffff !important;
  color: #000000 !important;
  /* Adjusting font name to match system-installed custom fonts */
  font-family: "Yu Gothic Bold", "Yu Gothic", "YuGothic", sans-serif !important;
  font-weight: 700 !important; 
  border: 2px solid #000000 !important;
  box-shadow: none !important;
  padding: 12px !important;
}

/* Force all child elements (definitions, tags) to use the bold font */
.yomitan-popup * {
  background: transparent !important;
  color: #000000 !important;
  font-family: inherit !important;
  font-weight: inherit !important;
  text-shadow: none !important;
}

/* --- 2. HEADWORD & FURIGANA (VERTICAL STACK) --- */
.headword {
  display: flex !important;
  flex-direction: column !important; /* Forces reading above the term */
  align-items: flex-start !important;
  margin-bottom: 8px !important;
}

/* The Kanji/Term */
.headword-term {
  font-size: 1.8em !important;
  line-height: 1.1 !important;
}

/* The Reading (Furigana) */
.yomitan-popup ruby rt, 
.headword-reading {
  font-size: 0.7em !important; /* Slightly larger than standard furigana for E-ink */
  line-height: 1 !important;
  margin-bottom: 2px !important;
}

/* --- 3. THE "MERGED" FREQUENCY BOXES --- */
/* Target the container to stop it from adding space between boxes */
.tag-list, .tag-group, .headword-tag-list {
  display: flex !important;
  flex-wrap: wrap !important;
  gap: 0 !important; 
}

.tag[data-category="frequency"] {
  display: inline-block !important;
  border: 1px solid #000000 !important;
  margin: 0 !important; 
  margin-right: -1px !important; /* This "staples" the borders together */
  padding: 2px 6px !important;
  font-size: 0.75rem !important;
  border-radius: 0 !important;
}

/* --- 4. THE JITINDEX CLEANUP --- */
/* Completely removes the dictionary name/header */
header, 
.dict-label, 
[data-dictionary*="Jitendex"], 
[data-dictionary*="jitindex"],
.tag[data-category="priority"] {
  display: none !important;
}

/* --- 5. HIGH-READABILITY DEFINITIONS --- */
.gloss-list {
  margin-top: 10px !important;
  border-top: 1px solid #000 !important;
  padding-top: 12px !important;
}

.gloss-item {
  font-size: 1.2rem !important; /* Larger for easy reading */
  line-height: 1.6 !important; /* High spacing for E-ink clarity */
  margin-bottom: 12px !important;
  list-style-position: outside !important;
}

/* Ensure bullet points/numbers are visible and black */
.yomitan-popup ol, .yomitan-popup ul {
  padding-left: 25px !important;
  list-style-type: decimal !important;
}